/**
 * Placeholder shell. The login page and authenticated layout land in M1.2/M1.4;
 * this exists so the build, typecheck, and Compose wiring are verified now
 * rather than discovered broken later.
 */
export default function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Adaptive Enterprise AI</h1>
      <p>Milestone 1 — scaffold. Authentication arrives in M1.2.</p>
    </main>
  );
}
